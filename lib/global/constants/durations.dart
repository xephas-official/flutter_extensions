/// Animation duration constants
library;

const oneSecond = Duration(seconds: 1);
const halfSecond = Duration(milliseconds: 500);
const quarterSecond = Duration(milliseconds: 250);
const twoSeconds = Duration(seconds: 2);
const threeSeconds = Duration(seconds: 3);

// Common animation durations
const shortDuration = Duration(milliseconds: 200);
const mediumDuration = Duration(milliseconds: 300);
const longDuration = Duration(milliseconds: 500);
