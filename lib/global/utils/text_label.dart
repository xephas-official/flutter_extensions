/// this is just a text label that we shall use in the UIs when we need to show a label and a value via extensions
class TextLabel {
  /// constructor
  TextLabel({
    required this.label,
    required this.value,
  });

  /// label
  final String label;

  /// value
  final String value;
}
